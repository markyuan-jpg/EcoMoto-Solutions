import type { Metadata } from "next";
import { Inter } from "next/font/google";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "EcoMoto Solutions - Electric Commercial Vehicles",
  description: "B2B electric commercial vehicles for your business. Sustainable, efficient, and cost-effective transportation solutions.",
  icons: {
    icon: "/favicon.ico",
  },
  openGraph: {
    title: "EcoMoto Solutions - Electric Commercial Vehicles",
    description: "Your trusted B2B partner for electric vehicles and battery solutions.",
    url: "https://www.ecomotosolutions.com",
    siteName: "EcoMoto Solutions",
    images: [
      {
        url: "/images/og-image.jpg",
        width: 1200,
        height: 630,
      },
    ],
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col bg-white text-slate-900">
        <Navbar />
        <main className="flex-1 pt-16">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
